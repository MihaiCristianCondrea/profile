/** Live store metadata for the Smart Cleaner app. Fields are null when the
 *  service does not supply a usable value. */
export interface SmartCleanerAppMetadata {
  shortDescription: string | null;
  iconUrl: string | null;
}

You are working on Mihai Cristian's profile public project. it is a clean architecture project that usese data domain presentation architecture with material web components material 3.

This project has many flaws at this moment and we need to fix it work on it and so on. You are running now in android studio so the only way to work on it is to edit the actual code with no npm because we do not have it. you can try npm build if is really needed to see what happens

The project has an uneditable source inside `resources/` folder. there we have our first type script project clean architecture that is an etalon, it is the model we will follow. it has nice UI ux, it uses the material web components, it has its own docs, agents.md and so on. and we can use that one because would be very good.
Github dev tools will influence how we work on our project. [github_dev_tools](../../resources/github_dev_tools) would be good to copy some of the docs of github dev tools to ours too

inside resources we have the material web components where the material web components latest version is. i added that because i want to ensure you are using the components correclly. without extra styling them.

for example. the `view ranking` button when is hovered it zooms in a bit. material do not do that. see? that's extra code that we do not need it.

---

I want to remove the extra styling we do for our project and firx the structure for the moment and the architecture of the project.

I want to make the ui more consistnent with the github dev tools. get the "Privacy Policy · Code of Conduct" they do not look consisntnet. and there are many other things we need to fix pls
